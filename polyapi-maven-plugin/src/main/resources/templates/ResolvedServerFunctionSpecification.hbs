package {{packageName}};

import io.polyapi.client.api.model.function.PolyServerFunction;
import io.polyapi.client.api.model.PolyEntity;
import io.polyapi.client.api.model.PolyMetadata;
{{~#each imports}}
import {{this}};
{{~/each}}

/**
 * Poly server function with ID {{id}}.
 */
@PolyEntity("{{id}}")
@PolyMetadata(paramNames = { {{{paramNames}}} },
    paramTypes = { {{{paramTypes}}} })
public interface {{className}} extends PolyServerFunction {

    {{{returnType}}} {{{methodSignature}}};
}
