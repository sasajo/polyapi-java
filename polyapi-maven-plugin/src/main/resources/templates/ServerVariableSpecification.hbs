package {{packageName}};

import io.polyapi.client.api.model.PolyEntity;
import io.polyapi.client.api.model.variable.{{~#if variable.secret}}ServerVariable{{~else}}RetrievableServerVariable{{/if}};

@PolyEntity("{{id}}")
public interface {{className}} extends{{~#if variable.secret}} ServerVariable{{~else}} RetrievableServerVariable{{/if}}<{{type}}> {

{{~#if variable.secret}}
{{~else}}
    @Override
    {{type}} get();
{{/if}}

    @Override
    void update({{type}} value);

}
