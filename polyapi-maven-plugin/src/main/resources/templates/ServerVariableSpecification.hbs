package {{packageName}};

import java.util.function.Consumer;

import org.json.JSONException;
import org.json.JSONObject;

import com.fasterxml.jackson.core.type.TypeReference;
import io.polyapi.client.api.*;

import io.polyapi.client.api.model.PolyEntity;
import io.polyapi.client.api.model.variable.{{~#if variable.secret}}ServerVariable{{~else}}RetrievableServerVariable{{/if}};

@PolyEntity("{{id}}")
public interface {{className}} extends{{~#if variable.secret}} ServerVariable {{~else}} RetrievableServerVariable {{/if}}{
  // No additional content.
}
