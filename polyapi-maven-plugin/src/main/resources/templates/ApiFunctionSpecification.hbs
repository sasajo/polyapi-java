package {{packageName}};

import io.polyapi.client.api.model.function.PolyApiFunction;
import io.polyapi.client.api.model.PolyEntity;
import io.polyapi.client.api.model.PolyMetadata;

@PolyEntity("{{id}}")
@PolyMetadata(paramNames = { {{~#each function.arguments}}"{{inCodeName}}"{{#unless @last}}, {{/unless}} {{~/each}} },
paramTypes = { {{~#each function.arguments}}"{{{type}}}"{{#unless @last}}, {{/unless}} {{~/each}} })
public interface {{className}} extends PolyApiFunction {

{{{resultType}}} {{name}}({{{renderArguments function.arguments}}});
}
