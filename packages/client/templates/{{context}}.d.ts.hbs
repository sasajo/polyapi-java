import poly = require("./index");

type UnregisterWebhookEventListener = () => void;
type AuthFunctionCallback = (token?: string, url?: string, error?: any) => any;

{{{typeDeclarations}}}
declare module "./index" {
  interface {{interfaceName}} {
    {{#each subContexts}}
      {{name}}: {{interfaceName}};
    {{/each}}
    {{#each functionDeclarations}}
      {{#if comment}}
      /**
      {{{comment}}}
      */
      {{/if}}
      {{name}}({{#each arguments}}{{name}}{{#unless required}}?{{/unless}}: {{{type}}}{{#unless @last}}, {{/unless}}{{/each}}): {{{returnType}}};
    {{/each}}
  }
}
