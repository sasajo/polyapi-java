module.exports = function(poly, sendLocalErrorMessage, ...mainArgs) {
  {{{code}}}

  try {
    return {{name}}.call(this, ...mainArgs);
  } catch (e) {
    if (sendLocalErrorMessage(e.message)) {
      return;
    }
    throw e;
  }
}
