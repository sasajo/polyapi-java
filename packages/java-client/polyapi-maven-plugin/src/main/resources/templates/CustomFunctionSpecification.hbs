package {{packageName}};

import io.polyapi.commons.api.model.PolyObject;
import io.polyapi.client.api.model.PolyEntity;
import io.polyapi.client.api.model.PolyMetadata;

// {{language}}
@PolyEntity("{{id}}")
@PolyMetadata(paramNames = { {{~#each function.arguments}}"{{inCodeName}}"{{#unless @last}}, {{/unless}} {{~/each}} },
        paramTypes = { {{~#each function.arguments}}"{{{type.inCodeName}}}"{{#unless @last}}, {{/unless}} {{~/each}} })
public class {{className}} implements PolyObject {

  public  {{~#if function.returnsValue}} {{function.returnType}}{{~else}} void{{~/if}} {{name}}({{{renderArguments function.arguments}}}) {
    {{{code}}}
  }
}
