{
  "name": "@poly/model",
  "version": "0.1.0",
  "description": "The package containing common model",
  "main": "build/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prebuild": "yarn run lint",
    "lint": "eslint src --ext ts"
  },
  "dependencies": {
    "class-validator": "^0.14.0",
    "cron-validator": "^1.3.1"
  }
}
